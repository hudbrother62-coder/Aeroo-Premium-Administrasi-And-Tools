'use client';import{useEffect,useState}from'react';export default function Page(){const[data,setData]=useState<any[]>([]);useEffect(()=>{fetch('/api/journals').then(r=>r.json()).then(setData)},[]);return <><div className="row between"><div><h1>Jurnal Kegiatan</h1><p className="muted">Dokumentasi materi, hasil, kendala dan tindak lanjut kegiatan kelompok.</p></div><a href="/jurnal/buat"><button className="btn">+ Buat Jurnal</button></a></div><div className="list section">{data.length?data.map(j=><article className="item" key={j.id}><span className="badge">{j.journal_date}</span><h3>{j.title}</h3><div className="muted">{j.activity_types?.name||'Kegiatan'}{j.person_in_charge?' • PJ: '+j.person_in_charge:''}</div><p>{j.summary||'Belum ada ringkasan.'}</p></article>):<div className="card">Belum ada jurnal kegiatan.</div>}</div></>}
