'use client';import{useState}from'react';export default function Page(){const now=new Date().toISOString().slice(0,10);const[from,setFrom]=useState(now),[to,setTo]=useState(now),[aud,setAud]=useState(''),[report,setReport]=useState<any>(null);const preview=()=>fetch(`/api/reports?from=${from}&to=${to}${aud?'&audience='+aud:''}`).then(r=>r.json()).then(setReport);return <><h1>Laporan</h1><p className="muted">Rentang tanggal bebas: satu hari, lintas bulan, hingga lima bulan atau lebih.</p><div className="two section"><section className="card"><h2>Filter Laporan</h2><div className="formGrid"><label>Mulai<input type="date" className="input" value={from} onChange={e=>setFrom(e.target.value)}/></label><label>Sampai<input type="date" className="input" value={to} onChange={e=>setTo(e.target.value)}/></label><label>Kategori<select className="select" value={aud} onChange={e=>setAud(e.target.value)}><option value="">Semua</option><option>KELOMPOK</option><option>MUDA_MUDI</option><option>CABERAWIT</option><option>IBU_IBU</option><option>PENGURUS</option></select></label></div><button className="btn section" onClick={preview}>Preview Laporan</button></section><section className="card"><h2>Export</h2><p className="muted">Arsitektur ekspor menggunakan dataset preview yang sama.</p><div className="row"><button className="btn secondary">Excel</button><button className="btn secondary">Word</button><button className="btn secondary">PDF</button></div></section></div>{report&&<section className="card section"><div style={{textAlign:'center'}}><div className="brand">AEROO PREMIUM ADMINISTRASI</div><h2>LAPORAN REKAP PRESENSI</h2><p>{report.period.from} — {report.period.to}</p></div><div className="grid section">{[['Pertemuan',report.summary.meetings],['Hadir',report.summary.H],['Izin',report.summary.I],['Alfa',report.summary.A]].map(([k,v])=><div className="card stat" key={String(k)}><span className="muted">{k}</span><strong>{v}</strong></div>)}</div></section>}</>}
