'use client';import {useEffect,useState}from'react';
export default function Page(){const[data,setData]=useState<any[]>([]);const[q,setQ]=useState('');const load=()=>fetch('/api/members?q='+encodeURIComponent(q)).then(r=>r.json()).then(setData);useEffect(()=>{void load()},[]);return <><div className="row between"><div><h1>Database Kelompok</h1><p className="muted">Master anggota termasuk Muda-Mudi, Ibu-Ibu dan Pengurus.</p></div><a href="/database/kelompok/tambah"><button className="btn">+ Tambah</button></a></div><div className="row section"><input className="input" placeholder="Cari anggota..." value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()}/><button className="btn secondary" onClick={load}>Cari</button></div><div className="chips"><span className="chip active">Semua</span><span className="chip">Muda-Mudi</span><span className="chip">Ibu-Ibu</span><span className="chip">Pengurus</span></div><div className="list">{data.length?data.map(x=><div className="item row between" key={x.id}><div><strong>{x.name}</strong><div className="muted">{x.member_categories?.map((c:any)=>c.categories?.name).filter(Boolean).join(' • ')||'Kelompok'}</div></div><span className="badge">{x.status==='ACTIVE'?'Aktif':'Nonaktif'}</span></div>):<div className="card">Belum ada anggota. Tambahkan anggota pertama untuk mulai menggunakan presensi.</div>}</div></>}
