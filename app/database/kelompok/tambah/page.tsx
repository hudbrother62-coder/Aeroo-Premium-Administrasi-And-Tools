'use client';import {FormEvent,useEffect,useState} from 'react';
type Cat={id:string;name:string};
export default function Page(){const[cats,setCats]=useState<Cat[]>([]);const[saving,setSaving]=useState(false);const[error,setError]=useState('');const[form,setForm]=useState({name:'',gender:'',birth_place:'',birth_date:'',phone:'',address:'',notes:'',category_ids:[] as string[]});
useEffect(()=>{fetch('/api/categories').then(r=>r.json()).then(setCats)},[]);
const toggle=(id:string)=>setForm(v=>({...v,category_ids:v.category_ids.includes(id)?v.category_ids.filter(x=>x!==id):[...v.category_ids,id]}));
async function save(e:FormEvent){e.preventDefault();setSaving(true);setError('');const payload={...form,gender:form.gender||null,birth_place:form.birth_place||null,birth_date:form.birth_date||null,phone:form.phone||null,address:form.address||null,notes:form.notes||null,status:'ACTIVE'};const r=await fetch('/api/members',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const j=await r.json();if(!r.ok){setError(j.error||'Gagal menyimpan anggota.');setSaving(false);return}window.location.href='/database/kelompok'}
return <><div className="pageHeader"><div><div className="eyebrow">Database Kelompok</div><h1>Tambah Anggota</h1><p>Data anggota menjadi sumber presensi dan rekap kelompok.</p></div></div><form className="card" onSubmit={save}><div className="formGrid"><label>Nama lengkap<input className="input" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Jenis kelamin<select className="select" value={form.gender} onChange={e=>setForm({...form,gender:e.target.value})}><option value="">Pilih</option><option value="L">Laki-laki</option><option value="P">Perempuan</option></select></label><label>Tempat lahir<input className="input" value={form.birth_place} onChange={e=>setForm({...form,birth_place:e.target.value})}/></label><label>Tanggal lahir<input type="date" className="input" value={form.birth_date} onChange={e=>setForm({...form,birth_date:e.target.value})}/></label><label>Nomor HP<input className="input" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Alamat<textarea className="textarea" value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></label></div><div className="section"><div className="sectionTitle"><div><h2>Kategori</h2><p>Satu anggota boleh memiliki lebih dari satu kategori.</p></div></div><div className="chips">{cats.map(c=><button type="button" key={c.id} className={form.category_ids.includes(c.id)?'chip active':'chip'} onClick={()=>toggle(c.id)}>{c.name}</button>)}</div></div><label style={{display:'grid',gap:7,marginTop:16,fontSize:12,fontWeight:700,color:'var(--muted)'}}>Catatan<textarea className="textarea" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label>{error&&<div className="notice error section">{error}</div>}<div className="row section"><a className="btn ghost" href="/database/kelompok">Batal</a><button className="btn" disabled={saving}>{saving?'Menyimpan…':'Simpan Anggota'}</button></div></form></>}