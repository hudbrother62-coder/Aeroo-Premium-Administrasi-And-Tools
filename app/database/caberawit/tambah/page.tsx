'use client';import{FormEvent,useEffect,useState}from'react';type Level={id:string;name:string};
export default function Page(){const[levels,setLevels]=useState<Level[]>([]);const[saving,setSaving]=useState(false);const[error,setError]=useState('');const[form,setForm]=useState({name:'',gender:'',birth_place:'',birth_date:'',guardian_name:'',guardian_phone:'',address:'',level_id:'',notes:''});useEffect(()=>{fetch('/api/levels').then(r=>r.json()).then(setLevels)},[]);
async function save(e:FormEvent){e.preventDefault();setSaving(true);setError('');const payload={...form,gender:form.gender||null,birth_place:form.birth_place||null,birth_date:form.birth_date||null,guardian_name:form.guardian_name||null,guardian_phone:form.guardian_phone||null,address:form.address||null,level_id:form.level_id||null,notes:form.notes||null,status:'ACTIVE'};const r=await fetch('/api/caberawit',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const j=await r.json();if(!r.ok){setError(j.error||'Gagal menyimpan.');setSaving(false);return}window.location.href='/database/caberawit'}
return <><div className="pageHeader"><div><div className="eyebrow">Database Caberawit</div><h1>Tambah Caberawit</h1><p>Data peserta tersimpan lokal di Aeroo dan tidak bergantung pada One Pro.</p></div></div><form className="card" onSubmit={save}><div className="formGrid"><label>Nama lengkap<input className="input" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Jenjang<select className="select" value={form.level_id} onChange={e=>setForm({...form,level_id:e.target.value})}><option value="">Pilih jenjang</option>{levels.map(l=><option key={l.id} value={l.id}>{l.name}</option>)}</select></label><label>Jenis kelamin<select className="select" value={form.gender} onChange={e=>setForm({...form,gender:e.target.value})}><option value="">Pilih</option><option value="L">Laki-laki</option><option value="P">Perempuan</option></select></label><label>Tanggal lahir<input type="date" className="input" value={form.birth_date} onChange={e=>setForm({...form,birth_date:e.target.value})}/></label><label>Nama wali<input className="input" value={form.guardian_name} onChange={e=>setForm({...form,guardian_name:e.target.value})}/></label><label>Nomor wali<input className="input" value={form.guardian_phone} onChange={e=>setForm({...form,guardian_phone:e.target.value})}/></label><label>Alamat<textarea className="textarea" value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></label><label>Catatan<textarea className="textarea" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label></div>{!levels.length&&<div className="notice section">Belum ada jenjang. <a href="/database/caberawit/jenjang"><strong>Buat jenjang dahulu</strong></a>.</div>}{error&&<div className="notice error section">{error}</div>}<div className="row section"><a className="btn ghost" href="/database/caberawit">Batal</a><button className="btn" disabled={saving}>{saving?'Menyimpan…':'Simpan Caberawit'}</button></div></form></>}