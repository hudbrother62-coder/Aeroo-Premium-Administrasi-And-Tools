'use client';import {useEffect,useState}from'react';export default function Page(){const[data,setData]=useState<any[]>([]);useEffect(()=>{fetch('/api/caberawit').then(r=>r.json()).then(setData)},[]);return <><div className="row between"><div><h1>Database Caberawit</h1><p className="muted">Database lokal Aeroo dengan jenjang dinamis.</p></div><a href="/database/caberawit/tambah"><button className="btn">+ Tambah</button></a></div><div className="row section"><input className="input" placeholder="Cari Caberawit..."/><button className="btn secondary">Jenjang</button></div><div className="list section">{data.length?data.map(x=><div className="item row between" key={x.id}><div><strong>{x.name}</strong><div className="muted">{x.levels?.name||'Jenjang belum dipilih'} • Wali: {x.guardian_name||'—'}</div></div><span className="badge">{x.status==='ACTIVE'?'Aktif':'Nonaktif'}</span></div>):<div className="card">Belum ada data Caberawit. Tambahkan peserta agar presensi dan laporan dapat digunakan.</div>}</div></>}
